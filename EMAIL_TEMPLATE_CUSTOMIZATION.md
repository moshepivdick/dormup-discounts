# 📧 Кастомизация шаблона email письма

## Как изменить содержимое письма подтверждения

Файл с шаблоном письма находится в: `lib/resend.ts`

### Основные параметры для изменения:

1. **Тема письма (subject):**
   ```typescript
   subject: 'Подтвердите ваш email адрес DormUp',
   ```

2. **HTML версия письма (emailHtml):**
   - Изменяется переменная `emailHtml` в функции `sendVerificationEmail`
   - Это визуальная версия письма с форматированием

3. **Текстовая версия (emailText):**
   - Изменяется переменная `emailText`
   - Это простая текстовая версия для почтовых клиентов без поддержки HTML

4. **Адрес отправителя:**
   - `FROM_EMAIL` - email адрес (по умолчанию: `onboarding@resend.dev`)
   - `FROM_NAME` - имя отправителя (по умолчанию: `DormUp`)
   - Настраиваются через переменные окружения:
     ```env
     RESEND_FROM_EMAIL=noreply@yourdomain.com
     RESEND_FROM_NAME=DormUp
     ```

### Структура HTML письма

Письмо состоит из:
- **Шапка** (зеленый блок с логотипом DormUp)
- **Основной контент** (приветствие, инструкции, кнопка)
- **Ссылка для копирования**
- **Футер** (информация об истечении ссылки)

### Пример изменения текста

Чтобы изменить текст письма, найдите в `lib/resend.ts`:

```typescript
const emailHtml = `
  ...
  <h2>Подтвердите ваш email адрес</h2>
  <p>Привет, ${name ? name : 'пользователь'}!</p>
  <p>Спасибо за регистрацию...</p>
  ...
`;
```

И измените нужные части.

### Изменение стилей

Все стили инлайновые (inline styles) для совместимости с почтовыми клиентами. Вы можете изменить:
- Цвета (background-color, color)
- Размеры шрифтов (font-size)
- Отступы (padding, margin)
- Границы (border, border-radius)

### Важно

- После изменения `lib/resend.ts` нужно перезапустить сервер
- Изменения применяются к новым письмам
- Старые письма не изменятся

## Быстрое редактирование

1. Откройте файл `lib/resend.ts`
2. Найдите функцию `sendVerificationEmail`
3. Измените нужные строки в `emailHtml` или `emailText`
4. Сохраните файл
5. Перезапустите сервер: `npm run dev`

