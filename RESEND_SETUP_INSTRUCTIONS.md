# 🚀 Полная инструкция по настройке Resend для DormUp

## ✅ Что уже сделано

1. ✅ Установлен пакет `resend` (уже в package.json)
2. ✅ Создана модель `EmailVerificationToken` в Prisma
3. ✅ Созданы утилиты для работы с токенами (`lib/email-verification.ts`)
4. ✅ Создан клиент Resend (`lib/resend.ts`)
5. ✅ Обновлен `signup` action для отправки email через Resend
6. ✅ Создан API route для подтверждения email (`/api/auth/verify-email`)
7. ✅ Создан API route для повторной отправки (`/api/auth/resend-verification`)
8. ✅ Обновлена страница `check-email` с кнопкой повторной отправки
9. ✅ Обновлена страница `login` для показа сообщений о верификации

## 📋 Что нужно сделать ВАМ

### Шаг 1: Создайте аккаунт Resend

1. Откройте https://resend.com
2. Нажмите "Sign Up" (регистрация бесплатна)
3. Заполните форму регистрации
4. Подтвердите email из письма

### Шаг 2: Создайте API ключ

1. Войдите в Resend Dashboard
2. Перейдите в раздел **API Keys** (слева в меню)
3. Нажмите кнопку **"Create API Key"**
4. Заполните:
   - **Name:** `DormUp Production` (или любое название)
   - **Permission:** `Sending access` (достаточно)
5. Нажмите **"Add"**
6. **ВАЖНО:** Скопируйте API ключ (показывается только один раз!)
   - Выглядит как: `re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### Шаг 3: Добавьте переменные окружения

Добавьте в ваш `.env` файл (локально):

```env
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=onboarding@resend.dev
RESEND_FROM_NAME=DormUp
```

**Также добавьте в Vercel:**
1. Откройте ваш проект в Vercel Dashboard
2. Перейдите в **Settings → Environment Variables**
3. Добавьте те же переменные:
   - `RESEND_API_KEY` = ваш API ключ
   - `RESEND_FROM_EMAIL` = `onboarding@resend.dev`
   - `RESEND_FROM_NAME` = `DormUp`

### Шаг 4: Примените миграцию базы данных

Выполните миграцию для создания таблицы `email_verification_tokens`:

**Вариант A: Через Prisma (если миграция работает):**
```bash
npx prisma migrate deploy
```

**Вариант B: Вручную в Supabase SQL Editor (рекомендуется):**
1. Откройте Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Скопируйте содержимое файла `prisma/migrations/20250116000000_add_email_verification_token/migration.sql`
4. Вставьте в SQL Editor и выполните

### Шаг 5: Отключите email подтверждение в Supabase

1. Откройте Supabase Dashboard
2. Перейдите в **Settings → Auth → Providers → Email**
3. **Отключите** "Confirm email" (это важно, так как мы используем Resend)
4. Сохраните

### Шаг 6: Перезапустите сервер

```bash
npm run dev
```

### Шаг 7: Протестируйте

1. Откройте http://localhost:3000/auth/signup
2. Зарегистрируйтесь с реальным email
3. Проверьте почту - должно прийти письмо от `onboarding@resend.dev`
4. Нажмите на ссылку в письме
5. Должно перенаправить на `/auth/login?verified=true`

## 🔍 Проверка работы

### Проверьте логи Resend

1. Откройте Resend Dashboard → **Logs**
2. Там должны быть видны все отправленные письма
3. Можно увидеть статус доставки

### Проверьте консоль сервера

При регистрации должны быть логи:
- `User created successfully: [user-id]`
- `Verification email sent successfully`

### Проверьте базу данных

1. Откройте Supabase Dashboard → **Table Editor**
2. Проверьте таблицу `email_verification_tokens`
3. После регистрации должен появиться токен
4. После подтверждения токен должен удалиться

## 📁 Структура файлов

```
lib/
  ├── resend.ts                    # Клиент Resend и функция отправки email
  └── email-verification.ts        # Функции для работы с токенами

app/
  ├── actions/
  │   └── auth.ts                  # Обновлен signup для отправки через Resend
  ├── api/
  │   └── auth/
  │       ├── verify-email/
  │       │   └── route.ts         # Обработка подтверждения email
  │       └── resend-verification/
  │           └── route.ts         # Повторная отправка email
  └── auth/
      ├── signup/
      │   └── page.tsx             # Страница регистрации
      ├── login/
      │   └── page.tsx             # Обновлена для показа сообщений
      └── check-email/
          └── page.tsx             # Обновлена с кнопкой повторной отправки

prisma/
  ├── schema.prisma                # Добавлена модель EmailVerificationToken
  └── migrations/
      └── 20250116000000_add_email_verification_token/
          └── migration.sql        # SQL миграция
```

## 🔄 Как это работает

1. **Регистрация:**
   - Пользователь заполняет форму регистрации
   - Создается пользователь в Supabase Auth (без email подтверждения)
   - Создается профиль в базе данных
   - Генерируется токен верификации
   - Отправляется email через Resend с ссылкой на подтверждение

2. **Подтверждение:**
   - Пользователь нажимает на ссылку в письме
   - Переходит на `/auth/verify-email?token=xxx`
   - API route проверяет токен
   - Если токен валидный:
     - Обновляется Supabase Auth пользователь (email подтвержден)
     - Обновляется профиль (verifiedStudent = true)
     - Токен удаляется
     - Редирект на `/auth/login?verified=true`

3. **Повторная отправка:**
   - Пользователь может запросить повторную отправку
   - Создается новый токен
   - Отправляется новое письмо

## 🎨 Кастомизация email шаблона

Чтобы изменить шаблон письма, отредактируйте файл `lib/resend.ts`:

- `emailHtml` - HTML версия письма
- `emailText` - текстовая версия письма
- `FROM_EMAIL` - адрес отправителя (можно использовать свой домен)
- `FROM_NAME` - имя отправителя

## 🚀 Для Production

### Использование своего домена (опционально, но рекомендуется)

1. В Resend Dashboard → **Domains** → **Add Domain**
2. Добавьте ваш домен (например, `dormup.com`)
3. Добавьте DNS записи, которые покажет Resend:
   - SPF запись
   - DKIM записи
   - MX запись (если нужно)
4. Подождите подтверждения (обычно несколько минут)
5. Измените `RESEND_FROM_EMAIL` на `noreply@yourdomain.com`

## ⚠️ Важные моменты

- **Не забудьте отключить email подтверждение в Supabase!** Иначе будут конфликты
- **API ключ Resend должен быть в `.env` файле** (и в Vercel для production)
- **Токены истекают через 24 часа** (можно изменить в `lib/email-verification.ts`)
- **Токены одноразовые** - после использования удаляются

## 🐛 Решение проблем

### Письма не отправляются

1. Проверьте что `RESEND_API_KEY` установлен в `.env`
2. Проверьте логи в Resend Dashboard → Logs
3. Проверьте консоль сервера на ошибки

### Токен не работает

1. Проверьте что миграция применена
2. Проверьте таблицу `email_verification_tokens` в Supabase
3. Проверьте что токен не истек (24 часа)

### "Email already verified"

1. Проверьте таблицу `profiles` - поле `verified_student`
2. Если `true`, значит email уже подтвержден

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи в Resend Dashboard
2. Проверьте консоль сервера
3. Проверьте Supabase Dashboard → Logs → Auth Logs
4. Убедитесь что все переменные окружения установлены

