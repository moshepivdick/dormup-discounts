# Настройка ADMIN_PANEL_PASSWORD_HASH на Vercel

## ✅ Что нужно сделать на Vercel

### 1. Сгенерируйте хеш пароля (если еще не сделали)

**Локально:**
```bash
npm run generate-admin-hash "ваш-пароль"
```

Скопируйте полученный хеш (60 символов, начинается с `$2b$10$`).

### 2. Добавьте переменную окружения в Vercel

1. **Откройте Vercel Dashboard:**
   - https://vercel.com/dashboard
   - Выберите проект `dormup-discounts`

2. **Перейдите в Settings → Environment Variables**

3. **Добавьте или обновите переменную:**
   - **Key:** `ADMIN_PANEL_PASSWORD_HASH`
   - **Value:** `$2b$10$...` (скопируйте хеш из шага 1)
   - **Environment:** ✅ Production, ✅ Preview, ✅ Development (все три!)

   ⚠️ **ВАЖНО:**
   - НЕ добавляйте кавычки вокруг значения
   - НЕ добавляйте пробелы в начале или конце
   - Хеш должен быть ровно 60 символов

4. **Сохраните** (Save)

### 3. Передеплойте проект

После добавления переменной окружения **обязательно передеплойте**:

**Вариант 1: Через Dashboard**
- Deployments → последний deployment → "Redeploy"
- Отключите "Use existing Build Cache"

**Вариант 2: Через Git**
- Сделайте любой commit и push
- Vercel автоматически передеплоит

### 4. Проверка после деплоя

После передеплоя проверьте логи в Vercel:
- Functions → Logs
- Найдите строку: `Reading ADMIN_PANEL_PASSWORD_HASH:`
- Должно быть:
  ```
  exists: true
  length: 60
  isEmpty: false
  ```

## 📋 Полный список переменных для Vercel

Убедитесь, что все эти переменные установлены:

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...

# Database
DATABASE_URL=...
DIRECT_URL=...

# JWT Secrets
PARTNER_JWT_SECRET=...
ADMIN_JWT_SECRET=...

# Admin Panel (⚠️ ЭТО НУЖНО ДОБАВИТЬ/ОБНОВИТЬ)
ADMIN_PANEL_SLUG=...
ADMIN_PANEL_PASSWORD_HASH=$2b$10$...  # ← ЭТА ПЕРЕМЕННАЯ
ADMIN_GATE_COOKIE_TTL_MINUTES=120

# Reports (если используется)
REPORT_TOKEN_SECRET=...
NEXT_PUBLIC_APP_URL=https://your-domain.vercel.app
```

## 🔍 Проверка работы

После настройки:
1. Откройте админ-панель на production
2. Введите пароль, который использовали при генерации хеша
3. Ошибка "Invalid password configuration" должна исчезнуть

## ❌ Если проблема останется

1. **Проверьте значение в Vercel:**
   - Убедитесь, что длина = 60 символов
   - Убедитесь, что начинается с `$2b$10$`
   - Убедитесь, что нет лишних пробелов

2. **Проверьте окружения:**
   - Production ✅
   - Preview ✅
   - Development ✅

3. **Проверьте логи:**
   - Functions → Logs в Vercel
   - Ищите сообщения об ошибках

4. **Передеплойте еще раз:**
   - Иногда нужно передеплоить после изменения переменных
